# Core dependencies for Quorum-MCP
# For production use, install via: pip install -e .
# For development, install via: pip install -e ".[dev]"

# MCP SDK
mcp>=0.9.0

# AI Provider SDKs
anthropic>=0.39.0
openai>=1.54.0

# Data validation and serialization
pydantic>=2.9.0

# Configuration and utilities
pyyaml>=6.0.0
python-dotenv>=1.0.0

# Async HTTP client
aiohttp>=3.10.0
